import type { DemoData } from '@/types'
import sampleData from '../../public/data/sample-products.json'

export const demoData: DemoData = {
  categories: sampleData.categories.map(cat => ({
    ...cat,
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  })),
  products: sampleData.products.map(prod => ({
    ...prod,
    title: prod.name,
    categoryId: prod.category,
    condition: 'new' as const,
    location: 'Hồ Chí Minh',
    sellerId: prod.seller.id,
    status: 'active' as const,
    badges: [],
    rating: prod.rating,
    reviewCount: prod.reviewCount,
    viewCount: 0,
    isFeatured: false,
    isPromoted: false,
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
    // Ensure seller object has all required fields
    seller: {
      id: prod.seller.id,
      name: prod.seller.name,
      email: 'seller@example.com',
      phone: '0123456789',
      avatar: prod.seller.avatar,
      roles: ['seller'] as const,
      postCount: 0,
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString(),
    },
    // Remove the old category field and ensure we have categoryId
    category: undefined,
  })),
  users: sampleData.users.map(user => ({
    ...user,
    roles: ['buyer'] as const,
    postCount: 0,
    updatedAt: new Date().toISOString(),
  })),
  orders: sampleData.orders.map(order => ({
    id: order.id,
    userId: 1, // Convert to number as required by Order interface
    storeId: 'store-1',
    status: order.status,
    paymentStatus: 'pending',
    paymentMethod: 'momo',
    subtotal: order.total,
    tax: 0,
    shipping: 0,
    discount: 0,
    total: order.total,
    currency: 'VND',
    shippingAddress: '123 Đường ABC, Quận 1, TP.HCM',
    billingAddress: '123 Đường ABC, Quận 1, TP.HCM',
    notes: '',
    storeName: 'TechStore Pro',
    storeLogo: '/store-logo.jpg',
    userEmail: 'user@example.com',
    userName: 'Nguyễn Văn A',
    items: order.items.map(item => ({
      id: `item-${Math.random()}`,
      orderId: order.id,
      productId: item.productId,
      productName: 'iPhone 14 Pro Max 256GB',
      productImage: 'https://images.unsplash.com/photo-1592899677977-9c10b588e3e9?w=80&h=80&fit=crop',
      price: item.price,
      quantity: item.quantity,
      subtotal: item.price * item.quantity,
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString(),
    })),
    createdAt: order.createdAt,
    updatedAt: new Date().toISOString(),
  })),
}